#!/usr/bin/env bash
set -euo pipefail

repository_root="$(git rev-parse --show-toplevel)"
cd "${repository_root}"
git config core.hooksPath .githooks
printf '%s\n' 'Git hooks 已启用：core.hooksPath=.githooks'
