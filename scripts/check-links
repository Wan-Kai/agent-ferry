#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
node_bin="$("${script_dir}/find-node")"
exec "${node_bin}" "${script_dir}/check-document-links.mjs" "$@"
